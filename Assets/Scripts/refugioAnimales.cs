using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class refugioAnimales : MonoBehaviour
{
    float alimentoGato = 300;
    float alimentoPerroPequeno = 400;
    float alimentoPerroGrande = 700;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
